name: flutter_devsecops_template
description: A Flutter DevSecOps template app.

version: 1.0.0+1

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Add other dependencies here

dev_dependencies:
  flutter_test:
    sdk: flutter
