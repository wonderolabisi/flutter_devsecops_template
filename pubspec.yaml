name: flutter_devsecops_template
description: A Flutter DevSecOps template app.

version: 1.0.0+1

environment:
  sdk: '>=3.8.0 <4.0.0'
  
dev_dependencies:
  flutter_test:
    sdk: flutter
    dev_dependencies:
  flutter_lints: ^3.0.0  # or the latest version
